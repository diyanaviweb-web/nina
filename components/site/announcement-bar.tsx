export function AnnouncementBar() {
  return (
    <div className="sticky top-0 z-50 flex h-9 items-center justify-center bg-primary px-4 text-center">
      <p className="text-[11px] font-medium uppercase tracking-[0.18em] text-primary-foreground">
        Complimentary design consultation — book your private appointment today
      </p>
    </div>
  )
}
